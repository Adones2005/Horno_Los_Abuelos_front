import { PedidoEstadoPipe } from './pedido-estado.pipe';

describe('PedidoEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new PedidoEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
